apiVersion: apps/v1
kind: Deployment
metadata:
  name: membership
  namespace: uhc-core
spec:
  replicas: 1
  selector:
    matchLabels: { app: membership }
  template:
    metadata:
      labels: { app: membership }
    spec:
      containers:
        - name: membership
          image: docker.io/ealen/echo-server:latest
          ports: [{ containerPort: 80 }]
          readinessProbe:
            httpGet: { path: "/", port: 80 }
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: "/", port: 80 }
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits:   { cpu: "250m", memory: "128Mi" }
---
containers:
  - name: app                # keep the existing name
    image: acruhcdev.azurecr.io/membership:0.1
apiVersion: v1
kind: Service
metadata:
  name: membership
  namespace: uhc-core
spec:
  selector: { app: membership }
  ports: [{ port: 80, targetPort: 80 }]
  type: ClusterIP
